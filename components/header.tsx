import React from 'react'

type Props = {}

function Header({}: Props) {
  return (
    <header className="bg-gradient-to-b from-blue-700 to-blue-500 px-4">
        Header
    </header>
  )
}

export default Header